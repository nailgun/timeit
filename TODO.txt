* l10n
* activity name and tags autocompletion
* client-side form validation
* ping status

* auth error handling (handle UserError exceptions)
* server-side error reporting (email, log)
* valid HTTP headers: content-length, content-type, charset
* clean database (not confirmed accounts, sessions, cache)

* uglify-js parameters
* cache middleware, per-page cache
* compress HTML
* client-side memory usage
* LESS
* template engine:
 * inheritance (extend)
 * async
 * include
 * clean syntax
 * escaping by default (overriden by filter)
 * __ shortcut

* node.js cluster
* interface:
  * animate activity name (zoom-in)
  * date and time pickers
  * no text selection in lists

* i18n module:
  * environment: production, dev (whether to update json files or not)
  * update all json files, not just en.json
  * link i18n locale to request
  * override language by user
  * client-side jQuery data-translate or class '__'
  * non-english plurals
